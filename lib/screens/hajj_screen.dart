import 'package:flutter/material.dart';
class HajjScreen extends StatelessWidget {
  const HajjScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Hajj Guide')), body: const Center(child: Text('Kommer snart...')));
}
